<UserControl
    x:Class="Ribbon.Controls.RibbonControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Ribbon.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Ribbon.ViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Ribbon;component/Themes/RibbonStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="RibbonTabToggleStyle" TargetType="ToggleButton">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border
                                Padding="8,6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="#CCCCCC" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Background" Value="#2D6FB2" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Foreground" Value="#333" />
                <Setter Property="Cursor" Value="Hand" />
            </Style>

            <DataTemplate x:Key="RibbonTabButtonTemplate" DataType="{x:Type vm:RibbonTabViewModel}">
                <ToggleButton
                    Margin="2,0"
                    Checked="TabButton_Checked"
                    IsChecked="{Binding IsSelected, Mode=TwoWay}"
                    IsEnabled="{Binding IsActive}"
                    Style="{StaticResource RibbonTabToggleStyle}">
                    <TextBlock
                        Padding="6,0"
                        FontWeight="SemiBold"
                        Text="{Binding Header}" />
                </ToggleButton>
            </DataTemplate>

            <DataTemplate x:Key="RibbonTabContentTemplate" DataType="{x:Type vm:RibbonTabViewModel}">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Groups}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Border Padding="4" Background="Transparent">
            <StackPanel>
                <Border
                    Margin="0,0,0,6"
                    Padding="4"
                    Background="Transparent"
                    BorderBrush="Beige"
                    BorderThickness="1">
                    <ItemsControl ItemTemplate="{StaticResource RibbonTabButtonTemplate}" ItemsSource="{Binding ViewModel.Tabs}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </Border>
                <ContentControl Content="{Binding ViewModel.SelectedTab}" ContentTemplate="{StaticResource RibbonTabContentTemplate}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
